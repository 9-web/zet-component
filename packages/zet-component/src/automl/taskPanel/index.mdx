---
name: Task (任务卡)
menu: AutoML
order: 70
---

import { Playground, PropsTable } from 'docz';
import { State, Toggle } from 'react-powerplug'
import { AutoML } from '../../index.tsx';
import jobList from './_mock/jobInfo.js';
import { LocaleProviderHoc } from "../../utils/hoc.tsx";


# AutoML.Design (建模设计)
自动建模建模设计时候用此组件


引入组件

```
import { AutoML } from 'zet-component';
```

## Basic task
<Playground>
  <State initial={{ show:'chart' }}>
    {
      ({state,setState})=>{
        const { Task } = AutoML;
        return LocaleProviderHoc(
          <Task
            title={'test01'}
            jobInfo={{jobId:'123',jobStatus:'FAIL'}}
            modelList={[
              {
                modelName:'test01',
                modelId:'1234',
                jobBlockStatus:5,
                score:0.8,
                modelTrainStatus:'success'
              },{
                modelName:'test02',
                modelId:'12345',
                jobBlockStatus:5,
                score:0.8,
                modelTrainStatus:'success'
              },
            ]}
          ></Task>
        )
      }
    }
  </State>
</Playground>

## task group

<Playground style={{height:500}}>
  <State initial={{ show:'chart' }}>
    {
      ({state,setState})=>{
        const { Task } = AutoML;
        const { Group } = Task;
        return LocaleProviderHoc(
          <Group selectedTaskId={'3ac15482-689f-4b0c-9212-0fd29086c46a'}>
             { jobList && jobList.map(item=>{
                return (
                  <Task
                    jobInfo={item}
                  ></Task>
                )
             })}
          </Group>
        )
      }
    }
  </State>
</Playground>
