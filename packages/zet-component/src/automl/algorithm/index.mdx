---
name: Alogrithm(算法)
menu: AutoML
---

import { Playground, PropsTable } from 'docz';
import { State, Toggle } from 'react-powerplug';
import { Button } from 'antd';
import { AutoML, LocaleProvider } from '../../index.tsx';
import { item, value } from './_mock/dataItem.js';
import list from './_mock/list.js';
import conditionList from './_mock/condition.js';

# Alogrithm

## 使用
Alogrithm


## 参数模板

### 组件输出接口数据
```
[
  {
    id: 'd1010bce-2050-4beb-855b-d9ebcae81598',
    name: 'VGG19',
    desc: 'VGG19',
    params: [{
      key: 'norm_size',
      name: 'norm_size4',
      extra: 'norm_size4',
      min: 48,
      max: 224,
      type: 'input-number',
      default: 48,
    }]
  }
]
```

## 案例介绍
<Playground>
  <State initial={{disabled: false, value: []}}>
    {({ state, setState }) => (
      <React.Fragment>
        <Button style={{marginBottom: '10px'}} type='primay' onClick={() => { setState({ disabled: !state.disabled}) }}>{state.disabled ? '启用' : '禁用'}</Button>
        <Button style={{marginBottom: '10px', marginLeft: '20px'}} type='primay' onClick={() => { setState({ value: [{"id":"component-item","name":"component-item","params":{"radioGroup":1,"timeSelect":1600}}]}) }}>设置Value受控</Button>
        <LocaleProvider locale={'zh_CN'}>
          <React.Fragment>
            <AutoML.Algorithm disabled={state.disabled} value={state.value} data={list} onChange={(value) => (setState({ value }))} />
            {/* <AutoML.Algorithm.Item value={state.value} data={item} onChange={(value) => (setState({ value }))} /> */}
            <span>
              change value: {JSON.stringify(state.value)}
            </span>
          </React.Fragment>
        </LocaleProvider>
      </React.Fragment>
    )}
  </State>
</Playground>


### condition(条件配置)
<Playground>
  <State initial={{disabled: false, value: []}}>
    {({ state, setState }) => (
      <React.Fragment>
        <Button style={{marginBottom: '10px'}} type='primay' onClick={() => { setState({ disabled: !state.disabled}) }}>{state.disabled ? '启用' : '禁用'}</Button>
        <LocaleProvider locale={'zh_CN'}>
          <React.Fragment>
            <AutoML.Algorithm disabled={state.disabled} value={state.value} data={conditionList} onChange={(value) => (setState({ value }))} />
            {/* <AutoML.Algorithm.Item value={state.value} data={item} onChange={(value) => (setState({ value }))} /> */}
            <span>
              change value: {JSON.stringify(state.value)}
            </span>
          </React.Fragment>
        </LocaleProvider>
      </React.Fragment>
    )}
  </State>
</Playground>


## API

<PropsTable of={AutoML.Algorithm} />
